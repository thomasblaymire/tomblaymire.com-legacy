import { StyledCallToAction, StyledText } from './styled';

const CallToAction = ({ text }) => (
  <StyledCallToAction>
    <StyledText>{text}</StyledText>
  </StyledCallToAction>
);

export default CallToAction;
